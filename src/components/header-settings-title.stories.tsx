import * as React from 'react';
import {storiesOf} from '@storybook/react-native';

import HeaderSettingsTitle from './header-settings-title';

storiesOf('HeaderSettingsTitle', module).add('Default', () => <HeaderSettingsTitle />);
