import * as React from 'react';
import {storiesOf} from '@storybook/react-native';

import HeartBroken from './heart-broken';

storiesOf('Heart Broken', module).add('Default', () => <HeartBroken />);
